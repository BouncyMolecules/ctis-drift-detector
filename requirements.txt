# =============================================================================
# Final requirements.txt — production deploy (Streamlit Community Cloud)
# =============================================================================
# Use with Advanced settings → Python 3.11. Lower bounds (>=) keep installs
# fast and wheel-based on Cloud’s manylinux image; avoid == pins unless you
# need byte-for-byte reproducibility outside Cloud.
# =============================================================================

# -----------------------------------------------------------------------------
# Streamlit Community Cloud — dependency policy
# -----------------------------------------------------------------------------
# Lower bounds (>=) let pip pick wheels built for the Cloud image's Python and
# platform. Exact pins (==) often force source builds when no matching wheel
# exists for that ABI—common pain points: pandas, numpy stack, SQLAlchemy.
# Functionality stays the same; resolver upgrades within compatible releases.
# Pin only if you need reproducible offline/validated builds elsewhere.
# -----------------------------------------------------------------------------
# Python: set Advanced settings → Python 3.11 in Streamlit Cloud when offered
# (matches pyproject.toml requires-python).
# -----------------------------------------------------------------------------

# UI & charts (>=1.55: layout widgets accept explicit ``key=`` for stable identity / mypy)
streamlit>=1.55.0
plotly>=5.24.0

# HTTP & env (CTIS transport via httpx)
httpx>=0.27.0

# Data & exports
# numpy: explicit floor aligns the numeric stack with pandas on py3.11 and
# reduces resolver weirdness on constrained Cloud runners.
numpy>=1.26.0
pandas>=2.2.0
openpyxl>=3.1.0
fpdf2>=2.8.0

# Config & persistence (SQLAlchemy is a direct import in storage; keep explicit)
pydantic>=2.10.0
pydantic-settings>=2.6.0
sqlmodel>=0.0.22
SQLAlchemy>=2.0.36
python-dotenv>=1.0.0
